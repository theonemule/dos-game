apiVersion: v1
kind: Service
metadata:
    name: "blaizek8s"
    labels:
        app: "blaizek8s"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "blaizek8s"