apiVersion: v1
kind: Service
metadata:
    name: "blaizek8s"
    labels:
        app: "blaizek8s"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "blaizek8s"